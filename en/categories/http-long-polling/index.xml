<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>HTTP Long Polling on 0AndWild_log</title><link>https://0andwild.com/en/categories/http-long-polling/</link><description>Recent content in HTTP Long Polling on 0AndWild_log</description><generator>Hugo -- gohugo.io</generator><language>en-US</language><lastBuildDate>Mon, 12 Jan 2026 21:18:15 +0900</lastBuildDate><atom:link href="https://0andwild.com/en/categories/http-long-polling/index.xml" rel="self" type="application/rss+xml"/><item><title>Bidirectional Communication Before WebSocket</title><link>https://0andwild.com/en/posts/260112_before_websocket/</link><pubDate>Mon, 12 Jan 2026 21:18:15 +0900</pubDate><guid>https://0andwild.com/en/posts/260112_before_websocket/</guid><description>&lt;img src="https://0andwild.com/" alt="Featured image of post Bidirectional Communication Before WebSocket" /&gt;&lt;h2 id="introduction"&gt;&lt;a href="#introduction" class="header-anchor"&gt;&lt;/a&gt;Introduction
&lt;/h2&gt;&lt;p&gt;While working on a mock investment project, I needed to serve real-time stock price fluctuation data and ended up using WebSocket technology.
I&amp;rsquo;ve known about WebSocket, a bidirectional real-time communication technology, for some time, but never dug deep into it. I took this opportunity to dive in thoroughly.&lt;/p&gt;
&lt;p&gt;While reading RFC 6455, the WebSocket technical document, I found a section that referenced RFC 6202 (Known Issues and Best Practices for the Use of Long Polling and Streaming in Bidirectional HTTP) to explain past processing methods and their problems.
Today, I&amp;rsquo;ll read RFC 6202 to learn how bidirectional communication was handled before WebSocket emerged, and explore the thoughts and best practices of that time.&lt;/p&gt;
&lt;hr&gt;
&lt;h2 id="rfc-6202-known-issues-and-best-practices-for-the-use-of-long-polling-and-streaming-in-bidirectional-http"&gt;&lt;a href="#rfc-6202-known-issues-and-best-practices-for-the-use-of-long-polling-and-streaming-in-bidirectional-http" class="header-anchor"&gt;&lt;/a&gt;&lt;a class="link" href="%22https://datatracker.ietf.org/doc/html/rfc6202%22" &gt;RFC 6202&lt;/a&gt; (Known Issues and Best Practices for the Use of Long Polling and Streaming in Bidirectional HTTP)
&lt;/h2&gt;&lt;p&gt;This document, written in April 2011, discusses HTTP long polling and HTTP streaming as well-known issues and best practices for bidirectional HTTP communication at that time. It also acknowledges that both HTTP long polling and HTTP streaming are extensions of HTTP, and that the HTTP protocol was not designed for bidirectional communication.
The authors note that this document neither recommends using these two methods nor discourages their use, but rather focuses on discussing good use cases and issues.&lt;/p&gt;
&lt;p&gt;Fundamentally, HTTP (Hypertext Transfer Protocol: RFC 2616) is a request/response protocol.
HTTP defines three entities: clients, proxies, and servers.
A client creates a connection to send an HTTP request to a server, and the server accepts the connection to process the HTTP request by returning a response. Proxies are objects that can intervene in forwarding requests and responses between clients and servers.&lt;/p&gt;
&lt;p&gt;By default, the standard HTTP model doesn&amp;rsquo;t allow servers to send asynchronous events to clients because servers cannot initiate connections to clients first and cannot send unsolicited HTTP responses.&lt;/p&gt;
&lt;p&gt;So to receive asynchronous events as quickly as possible, clients must periodically poll the server. This continuous polling forces request/response cycles even when there&amp;rsquo;s no data, consuming network resources and reducing application response efficiency as data queues up until the server receives the next polling request.&lt;/p&gt;
&lt;hr&gt;
&lt;h2 id="http-long-polling--http-streaming"&gt;&lt;a href="#http-long-polling--http-streaming" class="header-anchor"&gt;&lt;/a&gt;HTTP long polling &amp;amp; HTTP streaming
&lt;/h2&gt;&lt;h2 id="1-http-long-polling"&gt;&lt;a href="#1-http-long-polling" class="header-anchor"&gt;&lt;/a&gt;1. HTTP long polling
&lt;/h2&gt;&lt;p&gt;Traditional short polling is a technique where clients periodically send requests to the server to update data, but they receive empty responses even when there are no new events, or must wait until the next poll. This technique&amp;rsquo;s request cycle is determined by the delay time set by the client, and when this cycle is short (high polling frequency), it can cause unbearable burden on both server and network.&lt;/p&gt;
&lt;p&gt;In contrast, long polling attempts to minimize message delivery delays and network resource usage by responding to requests only when specific events, states, or network timeouts occur.&lt;/p&gt;
&lt;h3 id="http-long-polling-life-cycle"&gt;&lt;a href="#http-long-polling-life-cycle" class="header-anchor"&gt;&lt;/a&gt;HTTP long polling life cycle
&lt;/h3&gt;&lt;ol&gt;
&lt;li&gt;Client creates an initial request and waits for a response&lt;/li&gt;
&lt;li&gt;Server withholds response until updates are available or a specific state or timeout occurs&lt;/li&gt;
&lt;li&gt;When updates become available, server sends a response to the client&lt;/li&gt;
&lt;li&gt;Immediately after receiving the response, client either creates a new long poll request right away or after an acceptable delay&lt;/li&gt;
&lt;/ol&gt;
&lt;h3 id="http-long-polling-issues"&gt;&lt;a href="#http-long-polling-issues" class="header-anchor"&gt;&lt;/a&gt;HTTP long polling issues
&lt;/h3&gt;&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;Header overhead: Since every request/response is an HTTP message, HTTP headers always accompany it even if the data is small.
For small data, headers can constitute a significant portion of data transmission. If the network MTU (Maximum Transmission Unit) can accommodate all information including headers in a single IP packet, the network burden isn&amp;rsquo;t significant. However, when small messages are frequently exchanged, the problem of transmission volume being large relative to actual data occurs.
For example, it&amp;rsquo;s like sending a single sheet of paper (20g) in a delivery box (300g).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Maximal latency: Even if the server wants to immediately send a new message right after sending a long poll response, it must wait until the client&amp;rsquo;s next request arrives. Average latency is close to 1 network transit, but in the worst case can extend up to 3 network transits (response-request-response), and considering TCP packet loss retransmission, even more can occur.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Connection Establishment: Both short polling and long polling are criticized for frequently opening and closing TCP/IP connections. However, both polling mechanisms work well with persistent HTTP connections that can be reused.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Allocated Resources: Operating systems allocate resources to TCP/IP connections and pending HTTP requests. HTTP long polling requires both TCP/IP connections and HTTP requests to remain open for each client. Therefore, when determining the scale of an HTTP long polling application, it&amp;rsquo;s important to consider resources related to both.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Graceful Degradation: When servers or clients are overloaded, messages can queue up and multiple messages can be bundled in one response. Latency increases but per-message overhead decreases, naturally distributing the load.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Timeouts: Long poll requests can have timeout issues because they must maintain a hanging state until the server has data to send.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Caching: If intermediate proxies or CDNs cache responses, the problem of receiving old data instead of fresh data can occur. While clients or hosts have no way to inform HTTP intermediaries that long polling is in use, caching that could interfere with bidirectional flow can be controlled with standard headers or cookies. As a best practice, caching should always be intentionally suppressed in long polling requests or responses. Set the &amp;ldquo;Cache-Control&amp;rdquo; header to &amp;ldquo;no-cache&amp;rdquo;.&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;hr&gt;
&lt;h2 id="2-http-streaming"&gt;&lt;a href="#2-http-streaming" class="header-anchor"&gt;&lt;/a&gt;2. HTTP Streaming
&lt;/h2&gt;&lt;p&gt;The mechanism of HTTP streaming is to never terminate the request or disconnect the connection even after the server sends data to the client.
This mechanism significantly reduces network latency because clients and servers don&amp;rsquo;t need to continuously initiate and disconnect connections.&lt;/p&gt;
&lt;h3 id="http-streaming-life-cycle"&gt;&lt;a href="#http-streaming-life-cycle" class="header-anchor"&gt;&lt;/a&gt;HTTP Streaming life cycle
&lt;/h3&gt;&lt;ol&gt;
&lt;li&gt;Client creates an initial request and waits for a response&lt;/li&gt;
&lt;li&gt;Server withholds response until updates are available or a specific state or timeout occurs&lt;/li&gt;
&lt;li&gt;When updates become available, server sends a response to the client&lt;/li&gt;
&lt;li&gt;After sending data, server continues step 3 without terminating the request or disconnecting&lt;/li&gt;
&lt;/ol&gt;
&lt;h3 id="http-streaming-issues"&gt;&lt;a href="#http-streaming-issues" class="header-anchor"&gt;&lt;/a&gt;HTTP Streaming issues
&lt;/h3&gt;&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;Network Intermediaries: The HTTP protocol allows intermediaries (proxies, transparent proxies, gateways, etc.) to intervene in the process of transmitting responses from server to client. HTTP Streaming doesn&amp;rsquo;t work with these intermediaries.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Maximal Latency: Theoretically 1 network transit, but in practice, connections must be periodically disconnected and reconnected to prevent unlimited growth in memory usage related to Javascript/DOM elements. Ultimately, like long polling, maximum latency is 3 network transits.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Client Buffering: According to HTTP specifications, there&amp;rsquo;s no obligation to process partial responses immediately. While most browsers execute response JS, some only execute after buffer overflow occurs. Sending blank characters to fill the buffer can be used as a method.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Framing Techniques: When using HTTP Streaming, multiple application messages can be transmitted in a single HTTP response. However, because intermediate objects like proxies can re-chunk chunk units, messages cannot be distinguished by chunk units. Therefore, separators must be separately defined at the application level. Long polling doesn&amp;rsquo;t have this problem because there&amp;rsquo;s one message per response.&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;hr&gt;
&lt;h2 id="other-server-push-mechanisms"&gt;&lt;a href="#other-server-push-mechanisms" class="header-anchor"&gt;&lt;/a&gt;Other server-push mechanisms
&lt;/h2&gt;&lt;p&gt;Besides the two mechanisms above, this section introduces Bayeux (4.1), BOSH (4.2), Server-Sent Events (4.3), etc.
It covers recommendations when using the SSE mechanism, as follows:&lt;/p&gt;
&lt;p&gt;The specification recommends disabling HTTP chunking. The reason is the same as the HTTP streaming issues explained above.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Intermediate proxies can re-chunk chunks&lt;/li&gt;
&lt;li&gt;Some proxies can buffer entire responses&lt;/li&gt;
&lt;/ul&gt;
&lt;hr&gt;
&lt;h2 id="best-practices"&gt;&lt;a href="#best-practices" class="header-anchor"&gt;&lt;/a&gt;Best Practices
&lt;/h2&gt;&lt;h3 id="summary"&gt;&lt;a href="#summary" class="header-anchor"&gt;&lt;/a&gt;Summary
&lt;/h3&gt;&lt;table&gt;
 &lt;thead&gt;
 &lt;tr&gt;
 &lt;th&gt;Item&lt;/th&gt;
 &lt;th&gt;Key Content&lt;/th&gt;
 &lt;th&gt;Recommendation&lt;/th&gt;
 &lt;/tr&gt;
 &lt;/thead&gt;
 &lt;tbody&gt;
 &lt;tr&gt;
 &lt;td&gt;Connection Limit&lt;/td&gt;
 &lt;td&gt;6-8 per browser limit&lt;/td&gt;
 &lt;td&gt;Use one for long poll, detect duplicates with cookies&lt;/td&gt;
 &lt;/tr&gt;
 &lt;tr&gt;
 &lt;td&gt;Pipelining&lt;/td&gt;
 &lt;td&gt;Regular requests can get blocked behind long poll&lt;/td&gt;
 &lt;td&gt;Check support before use, prepare fallback&lt;/td&gt;
 &lt;/tr&gt;
 &lt;tr&gt;
 &lt;td&gt;Proxies&lt;/td&gt;
 &lt;td&gt;Starvation occurs when connections are shared&lt;/td&gt;
 &lt;td&gt;Use async proxies, avoid connection sharing&lt;/td&gt;
 &lt;/tr&gt;
 &lt;tr&gt;
 &lt;td&gt;Timeouts&lt;/td&gt;
 &lt;td&gt;Too high gets 408/504, too low wastes traffic&lt;/td&gt;
 &lt;td&gt;30 seconds recommended&lt;/td&gt;
 &lt;/tr&gt;
 &lt;tr&gt;
 &lt;td&gt;Caching&lt;/td&gt;
 &lt;td&gt;Real-time data shouldn&amp;rsquo;t be cached&lt;/td&gt;
 &lt;td&gt;&lt;code&gt;Cache-Control: no-cache&lt;/code&gt; mandatory&lt;/td&gt;
 &lt;/tr&gt;
 &lt;tr&gt;
 &lt;td&gt;Security&lt;/td&gt;
 &lt;td&gt;Vulnerable to Injection, DoS&lt;/td&gt;
 &lt;td&gt;Input validation, connection limit&lt;/td&gt;
 &lt;/tr&gt;
 &lt;/tbody&gt;
&lt;/table&gt;
&lt;hr&gt;
&lt;h2 id="1-limits-to-the-maximum-number-of-connections"&gt;&lt;a href="#1-limits-to-the-maximum-number-of-connections" class="header-anchor"&gt;&lt;/a&gt;1. Limits to the Maximum Number of Connections
&lt;/h2&gt;&lt;h3 id="background"&gt;&lt;a href="#background" class="header-anchor"&gt;&lt;/a&gt;Background
&lt;/h3&gt;&lt;p&gt;The HTTP specification (RFC 2616) originally recommended that &lt;strong&gt;a single client maintain a maximum of 2 connections&lt;/strong&gt; to a server. There are two reasons:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Prevent server overload&lt;/li&gt;
&lt;li&gt;Prevent unexpected side effects in congested networks&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Recent browsers have increased this limit to 6-8, but limits still exist. The problem is that users quickly exhaust these connections when opening multiple tabs or frames.&lt;/p&gt;
&lt;h3 id="why-is-this-a-problem"&gt;&lt;a href="#why-is-this-a-problem" class="header-anchor"&gt;&lt;/a&gt;Why is this a problem?
&lt;/h3&gt;&lt;p&gt;Long polling occupies connections for a long time. If 3 tabs each open 2 long polls:&lt;/p&gt;
&lt;pre tabindex="0"&gt;&lt;code&gt;Tab 1: 2 long poll connections
Tab 2: 2 long poll connections
Tab 3: 2 long poll connections
─────────────────────
Total 6 connections → Browser limit reached
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;In this state, to send regular HTTP requests (images, API calls, etc.), you must &lt;strong&gt;wait until existing connections end&lt;/strong&gt;. This is called &lt;strong&gt;connection starvation&lt;/strong&gt;.&lt;/p&gt;
&lt;h3 id="recommendations"&gt;&lt;a href="#recommendations" class="header-anchor"&gt;&lt;/a&gt;Recommendations
&lt;/h3&gt;&lt;p&gt;&lt;strong&gt;Client side:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Ideally, &lt;strong&gt;limit long poll requests to one&lt;/strong&gt; and have multiple tabs/frames share it&lt;/li&gt;
&lt;li&gt;However, sharing resources between tabs is difficult due to browser security models (Same-Origin Policy, etc.)&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;Server side:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Must use cookies to detect duplicate long poll requests from the same browser&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;When duplicate requests are detected, don&amp;rsquo;t make both wait—immediately respond to one to release it&lt;/li&gt;
&lt;/ul&gt;
&lt;pre tabindex="0"&gt;&lt;code&gt;[Wrong handling]
Request 1: Waiting...
Request 2: Waiting... ← Both waiting causes connection starvation

[Correct handling]
Request 1: Waiting...
Request 2: Arrives → Immediately send empty response to Request 1 → Only Request 2 waits
&lt;/code&gt;&lt;/pre&gt;&lt;hr&gt;
&lt;h2 id="2-pipelined-connections"&gt;&lt;a href="#2-pipelined-connections" class="header-anchor"&gt;&lt;/a&gt;2. Pipelined Connections
&lt;/h2&gt;&lt;h3 id="what-is-pipelining"&gt;&lt;a href="#what-is-pipelining" class="header-anchor"&gt;&lt;/a&gt;What is pipelining?
&lt;/h3&gt;&lt;p&gt;A feature supported in HTTP/1.1 that allows &lt;strong&gt;sending multiple requests consecutively&lt;/strong&gt; without waiting for responses.&lt;/p&gt;
&lt;pre tabindex="0"&gt;&lt;code&gt;[Without pipelining]
Request 1 → Response 1 → Request 2 → Response 2 → Request 3 → Response 3

[With pipelining]
Request 1 → Request 2 → Request 3 → Response 1 → Response 2 → Response 3
&lt;/code&gt;&lt;/pre&gt;&lt;h3 id="advantages-in-long-polling"&gt;&lt;a href="#advantages-in-long-polling" class="header-anchor"&gt;&lt;/a&gt;Advantages in Long Polling
&lt;/h3&gt;&lt;p&gt;Useful when the server needs to send multiple messages in a short time. With pipelining, the server doesn&amp;rsquo;t need to wait for the client&amp;rsquo;s new request after responding. Requests are already queued.&lt;/p&gt;
&lt;h3 id="problem-regular-requests-get-blocked"&gt;&lt;a href="#problem-regular-requests-get-blocked" class="header-anchor"&gt;&lt;/a&gt;Problem: Regular requests get blocked
&lt;/h3&gt;&lt;p&gt;There&amp;rsquo;s a critical problem with pipelining. &lt;strong&gt;If a regular request gets queued behind a long poll, it must wait until the long poll ends&lt;/strong&gt;.&lt;/p&gt;
&lt;pre tabindex="0"&gt;&lt;code&gt;[Pipeline queue]
1. Long poll request (waiting 30 seconds...)
2. Image request ← Wait until long poll ends
3. API request ← Wait until long poll ends
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;This can delay page loading by 30 seconds.&lt;/p&gt;
&lt;h3 id="precautions"&gt;&lt;a href="#precautions" class="header-anchor"&gt;&lt;/a&gt;Precautions
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;HTTP POST pipelining is &lt;strong&gt;not recommended&lt;/strong&gt; in RFC 2616&lt;/li&gt;
&lt;li&gt;Protocols like BOSH or Bayeux pipeline POSTs while guaranteeing order with request IDs&lt;/li&gt;
&lt;li&gt;To use pipelining, must &lt;strong&gt;verify that clients, intermediate equipment, and servers all support it&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;If not supported, must &lt;strong&gt;fallback&lt;/strong&gt; to non-pipelined method&lt;/li&gt;
&lt;/ul&gt;
&lt;hr&gt;
&lt;h2 id="3-proxies"&gt;&lt;a href="#3-proxies" class="header-anchor"&gt;&lt;/a&gt;3. Proxies
&lt;/h2&gt;&lt;h3 id="compatibility-with-general-proxies"&gt;&lt;a href="#compatibility-with-general-proxies" class="header-anchor"&gt;&lt;/a&gt;Compatibility with general proxies
&lt;/h3&gt;&lt;p&gt;&lt;strong&gt;Long Polling:&lt;/strong&gt; Works well with most proxies. Because it ultimately sends a complete HTTP response (when events occur or timeout).&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;HTTP Streaming:&lt;/strong&gt; Has problems. It relies on two assumptions:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Proxy will forward each chunk immediately → &lt;strong&gt;Not guaranteed&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;Browser will immediately execute arrived JS chunks → &lt;strong&gt;Not guaranteed&lt;/strong&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3 id="reverse-proxy-problems"&gt;&lt;a href="#reverse-proxy-problems" class="header-anchor"&gt;&lt;/a&gt;Reverse proxy problems
&lt;/h3&gt;&lt;p&gt;Reverse proxies appear as the actual server from the client&amp;rsquo;s perspective, but play the role of forwarding requests to the real server behind them.&lt;/p&gt;
&lt;pre tabindex="0"&gt;&lt;code&gt;Client → [Reverse Proxy] → Actual Server
 (Nginx, Apache, etc.)
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;Both long polling and streaming work, but there are &lt;strong&gt;performance issues&lt;/strong&gt;. Most proxies aren&amp;rsquo;t designed to maintain many connections for long periods.&lt;/p&gt;
&lt;h3 id="connection-sharing-problem"&gt;&lt;a href="#connection-sharing-problem" class="header-anchor"&gt;&lt;/a&gt;Connection Sharing Problem
&lt;/h3&gt;&lt;p&gt;This is the most serious problem. Proxies like Apache mod_jk are designed to have &lt;strong&gt;multiple clients share a small number of connections&lt;/strong&gt;.&lt;/p&gt;
&lt;pre tabindex="0"&gt;&lt;code&gt;[Apache mod_jk connection pool: 8 connections]

Client A&amp;#39;s long poll → Connection 1 occupied (30 seconds...)
Client B&amp;#39;s long poll → Connection 2 occupied (30 seconds...)
Client C&amp;#39;s long poll → Connection 3 occupied (30 seconds...)
...
Client H&amp;#39;s long poll → Connection 8 occupied (30 seconds...)

Client I&amp;#39;s regular request → No connection! Waiting...
Client J&amp;#39;s regular request → No connection! Waiting...
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;When all 8 connections are occupied by long polls, all other requests (whether long poll or regular) get blocked. This is called &lt;strong&gt;connection starvation&lt;/strong&gt;.&lt;/p&gt;
&lt;h3 id="root-cause-synchronous-vs-asynchronous"&gt;&lt;a href="#root-cause-synchronous-vs-asynchronous" class="header-anchor"&gt;&lt;/a&gt;Root cause: Synchronous vs Asynchronous
&lt;/h3&gt;&lt;table&gt;
 &lt;thead&gt;
 &lt;tr&gt;
 &lt;th&gt;Model&lt;/th&gt;
 &lt;th&gt;Operation&lt;/th&gt;
 &lt;th&gt;Long Poll Impact&lt;/th&gt;
 &lt;/tr&gt;
 &lt;/thead&gt;
 &lt;tbody&gt;
 &lt;tr&gt;
 &lt;td&gt;&lt;strong&gt;Synchronous&lt;/strong&gt;&lt;/td&gt;
 &lt;td&gt;One thread/connection per request&lt;/td&gt;
 &lt;td&gt;Severe resource exhaustion&lt;/td&gt;
 &lt;/tr&gt;
 &lt;tr&gt;
 &lt;td&gt;&lt;strong&gt;Asynchronous&lt;/strong&gt;&lt;/td&gt;
 &lt;td&gt;Event-based, minimal resources per connection&lt;/td&gt;
 &lt;td&gt;Minimal impact&lt;/td&gt;
 &lt;/tr&gt;
 &lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;Synchronous examples: Apache mod_jk, Java Servlet 2.5
Asynchronous examples: Nginx, Node.js, Java Servlet 3.0+&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Conclusion:&lt;/strong&gt; When using long polling/streaming, &lt;strong&gt;must avoid connection sharing&lt;/strong&gt;. HTTP&amp;rsquo;s basic assumption is &amp;ldquo;each request completes as quickly as possible,&amp;rdquo; but long poll breaks this assumption.&lt;/p&gt;
&lt;hr&gt;
&lt;h2 id="4-http-responses"&gt;&lt;a href="#4-http-responses" class="header-anchor"&gt;&lt;/a&gt;4. HTTP Responses
&lt;/h2&gt;&lt;p&gt;This is simple. Just follow standard HTTP.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;When server successfully receives a request, respond with &lt;strong&gt;200 OK&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;Response timing: event occurrence, state change, or timeout&lt;/li&gt;
&lt;li&gt;Response body includes actual event/state/timeout information&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Nothing special, just comply with HTTP specifications.&lt;/p&gt;
&lt;hr&gt;
&lt;h2 id="5-timeouts"&gt;&lt;a href="#5-timeouts" class="header-anchor"&gt;&lt;/a&gt;5. Timeouts
&lt;/h2&gt;&lt;h3 id="dilemma"&gt;&lt;a href="#dilemma" class="header-anchor"&gt;&lt;/a&gt;Dilemma
&lt;/h3&gt;&lt;p&gt;Setting long poll timeout values is tricky:&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;If set too high:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;May receive &lt;strong&gt;408 Request Timeout&lt;/strong&gt; from server&lt;/li&gt;
&lt;li&gt;May receive &lt;strong&gt;504 Gateway Timeout&lt;/strong&gt; from proxy&lt;/li&gt;
&lt;li&gt;Slow detection of disconnected network connections&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;If set too low:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Unnecessary requests/responses increase&lt;/li&gt;
&lt;li&gt;Network traffic waste&lt;/li&gt;
&lt;li&gt;Server load increase&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id="experimental-results-and-recommended-values"&gt;&lt;a href="#experimental-results-and-recommended-values" class="header-anchor"&gt;&lt;/a&gt;Experimental results and recommended values
&lt;/h3&gt;&lt;pre tabindex="0"&gt;&lt;code&gt;Browser default timeout: 300 seconds (5 minutes)
Values that succeeded in experiments: up to 120 seconds
Safe recommended value: 30 seconds
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;Most network infrastructure (proxies, load balancers, etc.) don&amp;rsquo;t have timeouts as long as browsers. Intermediate equipment can disconnect first.&lt;/p&gt;
&lt;h3 id="recommendations-for-network-equipment-vendors"&gt;&lt;a href="#recommendations-for-network-equipment-vendors" class="header-anchor"&gt;&lt;/a&gt;Recommendations for network equipment vendors
&lt;/h3&gt;&lt;p&gt;To be long polling compatible, timeouts must be set &lt;strong&gt;significantly longer than 30 seconds&lt;/strong&gt;. &amp;ldquo;Significantly&amp;rdquo; here means several times the average network round-trip time or more.&lt;/p&gt;
&lt;hr&gt;
&lt;h2 id="6-impact-on-intermediary-entities"&gt;&lt;a href="#6-impact-on-intermediary-entities" class="header-anchor"&gt;&lt;/a&gt;6. Impact on Intermediary Entities
&lt;/h2&gt;&lt;h3 id="transparency-problem"&gt;&lt;a href="#transparency-problem" class="header-anchor"&gt;&lt;/a&gt;Transparency problem
&lt;/h3&gt;&lt;p&gt;Long poll requests are &lt;strong&gt;indistinguishable from regular HTTP requests&lt;/strong&gt; from the perspective of intermediate equipment (proxies, gateways, etc.). There&amp;rsquo;s no way to tell them &amp;ldquo;this is a long poll, so handle it specially.&amp;rdquo;&lt;/p&gt;
&lt;p&gt;This can cause intermediate equipment to do unnecessary work:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Attempt caching (shouldn&amp;rsquo;t cache real-time data)&lt;/li&gt;
&lt;li&gt;Apply timeouts (long poll is supposed to take long)&lt;/li&gt;
&lt;li&gt;Attempt connection reuse (long poll has long occupation time)&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id="cache-prevention"&gt;&lt;a href="#cache-prevention" class="header-anchor"&gt;&lt;/a&gt;Cache prevention
&lt;/h3&gt;&lt;p&gt;The most important thing is &lt;strong&gt;cache prevention&lt;/strong&gt;. If real-time data is cached, clients receive past data.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Header that must be set:&lt;/strong&gt;&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-http" data-lang="http"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="err"&gt;Cache-Control: no-cache
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;This header must be included in both requests and responses. It&amp;rsquo;s a standard HTTP header, so most intermediate equipment understands and respects it.&lt;/p&gt;
&lt;hr&gt;
&lt;h2 id="7-security-considerations"&gt;&lt;a href="#7-security-considerations" class="header-anchor"&gt;&lt;/a&gt;7. Security Considerations
&lt;/h2&gt;&lt;p&gt;RFC 6202 is a document that describes existing usage patterns of HTTP, not proposing new features. Therefore, it doesn&amp;rsquo;t create new security vulnerabilities. However, there are security issues that exist in already deployed solutions.&lt;/p&gt;
&lt;h3 id="1-injection-attacks-cross-domain-long-polling"&gt;&lt;a href="#1-injection-attacks-cross-domain-long-polling" class="header-anchor"&gt;&lt;/a&gt;1. Injection attacks (Cross-Domain Long Polling)
&lt;/h3&gt;&lt;p&gt;&lt;strong&gt;Problem situation:&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;When using the JSONP method in cross-domain long polling, the browser executes JavaScript returned by the server.&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-javascript" data-lang="javascript"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="c1"&gt;// Server response (JSONP)
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nx"&gt;callback&lt;/span&gt;&lt;span class="p"&gt;({&lt;/span&gt;&lt;span class="s2"&gt;&amp;#34;price&amp;#34;&lt;/span&gt;&lt;span class="o"&gt;:&lt;/span&gt; &lt;span class="mi"&gt;52300&lt;/span&gt;&lt;span class="p"&gt;});&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;If the server is vulnerable to injection attacks, attackers can insert malicious code:&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-javascript" data-lang="javascript"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="c1"&gt;// Response manipulated by attacker
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="nx"&gt;callback&lt;/span&gt;&lt;span class="p"&gt;({&lt;/span&gt;&lt;span class="s2"&gt;&amp;#34;price&amp;#34;&lt;/span&gt;&lt;span class="o"&gt;:&lt;/span&gt; &lt;span class="mi"&gt;52300&lt;/span&gt;&lt;span class="p"&gt;});&lt;/span&gt; &lt;span class="nx"&gt;stealCookies&lt;/span&gt;&lt;span class="p"&gt;();&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;The browser executes this as is.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Countermeasures:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Thorough server-side input validation&lt;/li&gt;
&lt;li&gt;Use CORS and avoid JSONP&lt;/li&gt;
&lt;li&gt;Set Content-Type headers accurately&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id="2-dos-denial-of-service-attacks"&gt;&lt;a href="#2-dos-denial-of-service-attacks" class="header-anchor"&gt;&lt;/a&gt;2. DoS (Denial of Service) attacks
&lt;/h3&gt;&lt;p&gt;&lt;strong&gt;Problem situation:&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;Long polling and HTTP streaming must &lt;strong&gt;maintain many connections for long periods&lt;/strong&gt;. If attackers open a large number of long poll connections:&lt;/p&gt;
&lt;pre tabindex="0"&gt;&lt;code&gt;Attacker → Opens 1,000 connections (each waiting 30 seconds)
 ↓
Server resource exhaustion → Normal users cannot receive service
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;Regular HTTP requests end quickly, so resource occupation time per connection is short. But long poll is intentionally maintained for long periods, making it vulnerable to DoS.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Countermeasures:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Limit connections per IP&lt;/li&gt;
&lt;li&gt;Allow long poll only for authenticated users&lt;/li&gt;
&lt;li&gt;Apply rate limiting&lt;/li&gt;
&lt;li&gt;Use asynchronous servers (minimize resources per connection)&lt;/li&gt;
&lt;/ul&gt;
&lt;hr&gt;
&lt;h2 id="conclusion"&gt;&lt;a href="#conclusion" class="header-anchor"&gt;&lt;/a&gt;Conclusion
&lt;/h2&gt;&lt;p&gt;By reading RFC 6202, I learned how server push events were created in the past. It was good to learn in more detail about the knowledge and problems of polling, streaming, and SSE mechanisms that I already knew.&lt;/p&gt;
&lt;p&gt;The organized thought from reading this document is that it&amp;rsquo;s a concept of extending the HTTP protocol rather than a new protocol, and because HTTP by design is not a protocol for bidirectional asynchronous communication, it felt like they abused it to achieve the purpose of server event push.&lt;/p&gt;
&lt;p&gt;In the next post, I&amp;rsquo;ll organize the RFC 6455 WebSocket document and try to connect the problems from RFC 6202 with the development process.&lt;/p&gt;</description></item></channel></rss>